# Development scratch scripts

Non-CI helpers used while editing `scripts/generate_project.py` and related
templates. They are not part of the product runtime; keep new experiments here
instead of the repository root.
